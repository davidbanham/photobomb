!!!
body
  each item in items
    if item.type == 'image'
      a(href="images/#{item.name}")
        img(src="thumbnails/#{item.name}")
    if item.type == 'directory'
      a(href="gallery/#{item.name}")
        img(src="thumbnails/#{item.name}")
    br
